name: Notify Discord on file.json change

on:
  push:
    paths:
      - 'whitelist.json'  # цей параметр реагує тільки на зміни file.json

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification
        run: |
          curl -H "Content-Type: application/json" \
               -X POST \
               -d '{"content": "🚨 **Важлива зміна!** Файл `file.json` був змінений. Перевірте останні коміти та оновлення."}' \
               ${{ secrets.DISCORD_WEBHOOK }}
